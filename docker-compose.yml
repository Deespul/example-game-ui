version: '3.8'
services:
  example-game-ui:
    image: deespul/example-game-ui:${TAG}
    container_name: matchbox-example-game-ui
    restart: always
    expose:
      - 80
    environment:
      - VIRTUAL_HOST=demo.matchbox.gg
      - LETSENCRYPT_HOST=demo.matchbox.gg
      - LETSENCRYPT_EMAIL=simonheiss87@gmail.com
      - API_URL=${API_URL}
      - MATCHBOX_API=${MATCHBOX_API}

networks:
  default:
    external:
      name: nginx-proxy
